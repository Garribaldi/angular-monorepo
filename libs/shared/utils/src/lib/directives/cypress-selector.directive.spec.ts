import { CypressSelectorDirective } from './cypress-selector.directive';

describe('CypressSelectorDirective', () => {
  it('should create an instance', () => {
    const directive = new CypressSelectorDirective();
    expect(directive).toBeTruthy();
  });
});
